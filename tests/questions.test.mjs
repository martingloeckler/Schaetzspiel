import test from"node:test";import assert from"node:assert/strict";import{selectQuestions}from"../dist/game/questions.js";const qs=Array.from({length:12},(_,i)=>({id:`q${i}`,category:i<10?"A":"B",text:`Q${i}`,answer:i,inputType:"number"}));test("selection",()=>{const s=selectQuestions(qs,["A"],10,()=>.5);assert.equal(s.length,10);assert.equal(new Set(s.map(q=>q.id)).size,10);assert.throws(()=>selectQuestions(qs,["B"],10))});
