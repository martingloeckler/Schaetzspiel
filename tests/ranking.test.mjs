import test from"node:test";import assert from"node:assert/strict";import{rankAnswers,pointsForRank}from"../dist/game/ranking.js";const q={id:"q",category:"T",text:"100",answer:100,inputType:"number"},a=(id,v,t)=>({playerId:id,questionId:"q",value:v,responseTimeMs:t,submittedAt:t});test("ranking",()=>{const r=rankAnswers(q,[a("a",100,1000),a("b",99,2000),a("c",101,2000),a("d",95,1000)]);assert.deepEqual(r.map(x=>x.rank),[1,2,2,4]);assert.deepEqual(r.map(x=>x.points),[10,9,9,7]);assert.equal(pointsForRank(11),0)});
