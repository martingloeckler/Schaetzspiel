import type { Question } from "../types/game.js";
export function selectQuestions(questions:Question[],enabledCategories:string[],count=10,random:()=>number=Math.random):Question[]{const allowed=new Set(enabledCategories);const available=questions.filter(q=>allowed.has(q.category));if(enabledCategories.length===0)throw new Error("At least one category must be enabled.");if(available.length<count)throw new Error(`Not enough questions: ${available.length} available, ${count} required.`);const pool=[...available];for(let i=pool.length-1;i>0;i--){const j=Math.floor(random()*(i+1));[pool[i],pool[j]]=[pool[j],pool[i]];}return pool.slice(0,count);}
