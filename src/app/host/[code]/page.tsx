"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{getSocket}from"@/client/socket";import{Countdown}from"@/components/Countdown";export default function Host(){const{code}=useParams<{code:string}>(),c=code.toUpperCase(),[q,setQ]=useState<any>(),[count,setCount]=useState({count:0,total:0}),[round,setRound]=useState<any>(),[leader,setLeader]=useState<any[]>(),[done,setDone]=useState(false);useEffect(()=>{const s=getSocket();const question=(x:any)=>{setQ(x);setRound(null);setLeader(undefined);setCount({count:0,total:0})};s.on("question_started",question);s.on("answer_count_updated",setCount);s.on("round_results",setRound);s.on("leaderboard",(x:any)=>setLeader(x.entries));s.on("game_finished",(x:any)=>{setLeader(x.entries);setDone(true)});return()=>{s.off("question_started",question);s.off("answer_count_updated");s.off("round_results");s.off("leaderboard");s.off("game_finished")}},[]);const token=()=>localStorage.getItem(`schaetzspiel:host:${c}`);return <main className="page"><section className="shell">{done?<><h1>Finale 🏆</h1><Board rows={leader}/></>:leader?<><h1>Gesamtrangliste</h1><Board rows={leader}/><button className="button" onClick={()=>getSocket().emit("next_question",{joinCode:c,hostToken:token()})}>Nächste Frage</button></>:round?<><h1>Rundenergebnis</h1><div className="host-grid"><Board rows={round.rankedAnswers?.map((x:any)=>({rank:x.rank,name:x.playerName,totalScore:`+${x.points}`}))}/><div className="card stack"><div className="label">Richtige Antwort</div><h2>{round.correctAnswer}</h2><button className="button" onClick={()=>getSocket().emit("show_leaderboard",{joinCode:c,hostToken:token()})}>Gesamtrangliste</button></div></div></>:q?<div className="host-grid"><div className="card stack"><div className="kicker">Frage {q.questionNumber} / {q.totalQuestions}</div><div className="question">{q.question.text}</div></div><div className="card stack"><Countdown endsAt={q.questionEndsAt}/><h2>{count.count} / {count.total} Antworten</h2></div></div>:<div className="card">Warte auf Spielstart…</div>}</section></main>}
function Board({rows=[]}:{rows?:any[]}){return <div className="card leader">{rows?.map((x:any)=><div className="row" key={x.id??x.name}><b>{x.rank}.</b><span>{x.name}</span><b>{x.totalScore}</b></div>)}</div>}
