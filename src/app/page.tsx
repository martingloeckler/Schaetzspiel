"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function Home(){const r=useRouter(),[code,setCode]=useState("");return <main className="page"><section className="shell"><div className="kicker">Multiplayer-Schätzquiz</div><h1 className="brand">Schätzspiel</h1><div className="card stack"><button className="button" onClick={()=>r.push('/create')}>Spiel erstellen</button><div className="label">Oder Spiel beitreten</div><input className="input" maxLength={4} value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="Spielcode"/><button className="button secondary" onClick={()=>code&&r.push(`/join/${code}`)}>Beitreten</button></div></section></main>}
